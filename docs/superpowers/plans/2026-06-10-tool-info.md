# Tool Info Modal Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add an ℹ️ button to the ToolStrip that opens a modal listing all currently available tools with their enabled/disabled status and a short description.

**Architecture:** A new `ToolInfoModal.tsx` component owns the modal UI and Escape key handler. `ToolStrip.tsx` gains an `onInfo` prop and renders the ℹ️ button. `App.tsx` adds `isToolInfoOpen` state and wires the two together. No backend changes.

**Tech Stack:** React 19, TypeScript, Tailwind CSS 4

---

## File Map

| File | Change |
|---|---|
| `frontend/src/ToolInfoModal.tsx` | Create — modal component with tool list, descriptions, enabled/disabled badges |
| `frontend/src/ToolStrip.tsx` | Modify — add `onInfo` prop, render ℹ️ button after tool icons |
| `frontend/src/App.tsx` | Modify — add `isToolInfoOpen` state, wire `onInfo` and render `<ToolInfoModal>` |

---

### Task 1: Create `ToolInfoModal.tsx`

**Files:**
- Create: `frontend/src/ToolInfoModal.tsx`

- [ ] **Step 1: Create the file**

`frontend/src/ToolInfoModal.tsx`:

```tsx
const TOOL_META: Record<string, { icon: string; description: string }> = {
  web:        { icon: '🌐', description: 'Search the web and fetch URLs.' },
  unix:       { icon: '📁', description: 'Browse, read and search files in the selected project.' },
  'md-writer':{ icon: '✏️', description: 'Write spec and plan markdown files to the project.' },
};

interface ToolInfoModalProps {
  availableTools: string[];
  disabledTools: Set<string>;
  onClose: () => void;
}

export function ToolInfoModal({ availableTools, disabledTools, onClose }: ToolInfoModalProps) {
  useEffect(() => {
    const handler = (e: KeyboardEvent) => { if (e.key === 'Escape') onClose(); };
    document.addEventListener('keydown', handler);
    return () => document.removeEventListener('keydown', handler);
  }, [onClose]);

  return (
    <div
      className="fixed inset-0 bg-black/45 z-50 flex items-center justify-center"
      onClick={(e) => { if (e.target === e.currentTarget) onClose(); }}
    >
      <div className="bg-white rounded-xl w-[380px] max-w-[90vw] shadow-2xl overflow-hidden">
        <div className="px-5 py-4 border-b border-gray-100 flex justify-between items-center">
          <h2 className="text-sm font-semibold text-gray-900 m-0">Available tools</h2>
          <button
            onClick={onClose}
            aria-label="Close"
            className="text-gray-400 hover:text-gray-600 text-lg leading-none px-1.5 py-0.5 rounded"
          >
            ×
          </button>
        </div>
        <div className="px-5 py-4 flex flex-col gap-4">
          {availableTools.map((tool) => {
            const meta = TOOL_META[tool] ?? { icon: '🔧', description: '' };
            const disabled = disabledTools.has(tool);
            return (
              <div key={tool} className={`flex gap-3 ${disabled ? 'opacity-50' : ''}`}>
                <span className="text-2xl flex-shrink-0 mt-0.5">{meta.icon}</span>
                <div className="flex-1">
                  <div className="flex items-center gap-2 mb-0.5">
                    <span className="text-sm font-semibold text-gray-900">{tool}</span>
                    {disabled ? (
                      <span className="text-xs bg-red-100 text-red-800 px-2 py-0.5 rounded-full font-medium">disabled</span>
                    ) : (
                      <span className="text-xs bg-green-100 text-green-800 px-2 py-0.5 rounded-full font-medium">enabled</span>
                    )}
                  </div>
                  {meta.description && (
                    <p className="text-xs text-gray-500 m-0">{meta.description}</p>
                  )}
                </div>
              </div>
            );
          })}
        </div>
      </div>
    </div>
  );
}
```

Note: the file needs `import { useEffect } from 'react';` at the top.

Full file with import:

```tsx
import { useEffect } from 'react';

const TOOL_META: Record<string, { icon: string; description: string }> = {
  web:         { icon: '🌐', description: 'Search the web and fetch URLs.' },
  unix:        { icon: '📁', description: 'Browse, read and search files in the selected project.' },
  'md-writer': { icon: '✏️', description: 'Write spec and plan markdown files to the project.' },
};

interface ToolInfoModalProps {
  availableTools: string[];
  disabledTools: Set<string>;
  onClose: () => void;
}

export function ToolInfoModal({ availableTools, disabledTools, onClose }: ToolInfoModalProps) {
  useEffect(() => {
    const handler = (e: KeyboardEvent) => { if (e.key === 'Escape') onClose(); };
    document.addEventListener('keydown', handler);
    return () => document.removeEventListener('keydown', handler);
  }, [onClose]);

  return (
    <div
      className="fixed inset-0 bg-black/45 z-50 flex items-center justify-center"
      onClick={(e) => { if (e.target === e.currentTarget) onClose(); }}
    >
      <div className="bg-white rounded-xl w-[380px] max-w-[90vw] shadow-2xl overflow-hidden">
        <div className="px-5 py-4 border-b border-gray-100 flex justify-between items-center">
          <h2 className="text-sm font-semibold text-gray-900 m-0">Available tools</h2>
          <button
            onClick={onClose}
            aria-label="Close"
            className="text-gray-400 hover:text-gray-600 text-lg leading-none px-1.5 py-0.5 rounded"
          >
            ×
          </button>
        </div>
        <div className="px-5 py-4 flex flex-col gap-4">
          {availableTools.map((tool) => {
            const meta = TOOL_META[tool] ?? { icon: '🔧', description: '' };
            const disabled = disabledTools.has(tool);
            return (
              <div key={tool} className={`flex gap-3 ${disabled ? 'opacity-50' : ''}`}>
                <span className="text-2xl flex-shrink-0 mt-0.5">{meta.icon}</span>
                <div className="flex-1">
                  <div className="flex items-center gap-2 mb-0.5">
                    <span className="text-sm font-semibold text-gray-900">{tool}</span>
                    {disabled ? (
                      <span className="text-xs bg-red-100 text-red-800 px-2 py-0.5 rounded-full font-medium">disabled</span>
                    ) : (
                      <span className="text-xs bg-green-100 text-green-800 px-2 py-0.5 rounded-full font-medium">enabled</span>
                    )}
                  </div>
                  {meta.description && (
                    <p className="text-xs text-gray-500 m-0">{meta.description}</p>
                  )}
                </div>
              </div>
            );
          })}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Verify TypeScript compiles**

```
cd "C:/Users/Lenovo/IdeaProjects/agent-suite/frontend" && npx tsc -b --noEmit
```

Expected: zero errors.

- [ ] **Step 3: Commit**

```
git -C "C:/Users/Lenovo/IdeaProjects/agent-suite" add frontend/src/ToolInfoModal.tsx
git -C "C:/Users/Lenovo/IdeaProjects/agent-suite" commit -m "feat: add ToolInfoModal component"
```

---

### Task 2: Add ℹ️ button to `ToolStrip.tsx`

**Files:**
- Modify: `frontend/src/ToolStrip.tsx`

Current file content:
```tsx
const TOOL_META: Record<string, { icon: string; tooltip: string }> = {
  'unix':      { icon: '📁', tooltip: 'unix: ls · cat · grep' },
  'md-writer': { icon: '✏️', tooltip: 'md-writer: write markdown files' },
  'web':       { icon: '🌐', tooltip: 'web: search · fetch' },
};

interface ToolStripProps {
  availableTools: string[];
  disabledTools: Set<string>;
  onToggle: (tool: string) => void;
}

export function ToolStrip({ availableTools, disabledTools, onToggle }: ToolStripProps) {
  if (availableTools.length === 0) return null;

  return (
    <div className="bg-white border-t border-gray-100 px-4 py-1.5 flex gap-2 items-center">
      {availableTools.map((tool) => {
        const meta = TOOL_META[tool] ?? { icon: '🔧', tooltip: tool };
        const disabled = disabledTools.has(tool);
        return (
          <button
            key={tool}
            type="button"
            title={meta.tooltip}
            onClick={() => onToggle(tool)}
            aria-label={`Toggle ${meta.tooltip}${disabled ? ' — currently disabled' : ''}`}
            aria-pressed={!disabled}
            className={`relative p-1.5 rounded-md text-base leading-none cursor-pointer transition-all ${
              disabled ? 'bg-gray-100 opacity-40 grayscale' : 'bg-blue-100'
            }`}
          >
            {meta.icon}
            {disabled && (
              <span className="absolute inset-0 flex items-center justify-center pointer-events-none" aria-hidden="true">
                <span className="block w-4/5 bg-gray-500" style={{ height: '1.5px', transform: 'rotate(-35deg)' }} />
              </span>
            )}
          </button>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 1: Replace the full file with the updated version**

Replace the entire `frontend/src/ToolStrip.tsx` with:

```tsx
const TOOL_META: Record<string, { icon: string; tooltip: string }> = {
  'unix':      { icon: '📁', tooltip: 'unix: ls · cat · grep' },
  'md-writer': { icon: '✏️', tooltip: 'md-writer: write markdown files' },
  'web':       { icon: '🌐', tooltip: 'web: search · fetch' },
};

interface ToolStripProps {
  availableTools: string[];
  disabledTools: Set<string>;
  onToggle: (tool: string) => void;
  onInfo: () => void;
}

export function ToolStrip({ availableTools, disabledTools, onToggle, onInfo }: ToolStripProps) {
  if (availableTools.length === 0) return null;

  return (
    <div className="bg-white border-t border-gray-100 px-4 py-1.5 flex gap-2 items-center">
      {availableTools.map((tool) => {
        const meta = TOOL_META[tool] ?? { icon: '🔧', tooltip: tool };
        const disabled = disabledTools.has(tool);
        return (
          <button
            key={tool}
            type="button"
            title={meta.tooltip}
            onClick={() => onToggle(tool)}
            aria-label={`Toggle ${meta.tooltip}${disabled ? ' — currently disabled' : ''}`}
            aria-pressed={!disabled}
            className={`relative p-1.5 rounded-md text-base leading-none cursor-pointer transition-all ${
              disabled ? 'bg-gray-100 opacity-40 grayscale' : 'bg-blue-100'
            }`}
          >
            {meta.icon}
            {disabled && (
              <span className="absolute inset-0 flex items-center justify-center pointer-events-none" aria-hidden="true">
                <span className="block w-4/5 bg-gray-500" style={{ height: '1.5px', transform: 'rotate(-35deg)' }} />
              </span>
            )}
          </button>
        );
      })}
      <button
        type="button"
        onClick={onInfo}
        aria-label="Show tool info"
        title="Tool info"
        className="p-1.5 rounded-md text-base leading-none cursor-pointer bg-blue-50"
      >
        ℹ️
      </button>
    </div>
  );
}
```

- [ ] **Step 2: Verify TypeScript compiles**

```
cd "C:/Users/Lenovo/IdeaProjects/agent-suite/frontend" && npx tsc -b --noEmit
```

Expected: one error — `App.tsx` doesn't pass `onInfo` yet. That is expected at this step.

- [ ] **Step 3: Commit**

```
git -C "C:/Users/Lenovo/IdeaProjects/agent-suite" add frontend/src/ToolStrip.tsx
git -C "C:/Users/Lenovo/IdeaProjects/agent-suite" commit -m "feat: add onInfo prop and info button to ToolStrip"
```

---

### Task 3: Wire `ToolInfoModal` in `App.tsx`

**Files:**
- Modify: `frontend/src/App.tsx`

- [ ] **Step 1: Add the import for `ToolInfoModal`**

At the top of `frontend/src/App.tsx`, the current import line for `ToolStrip` is:
```tsx
import { ToolStrip } from './ToolStrip';
```

Add `ToolInfoModal` import on the next line:
```tsx
import { ToolStrip } from './ToolStrip';
import { ToolInfoModal } from './ToolInfoModal';
```

- [ ] **Step 2: Add `isToolInfoOpen` state**

Find the line:
```tsx
const [isAdmin, setIsAdmin] = useState(false);
```

Add `isToolInfoOpen` state directly after it:
```tsx
const [isAdmin, setIsAdmin] = useState(false);
const [isToolInfoOpen, setIsToolInfoOpen] = useState(false);
```

- [ ] **Step 3: Wire `onInfo` into `<ToolStrip>` and render `<ToolInfoModal>`**

Find the current `<ToolStrip>` usage:
```tsx
      <ToolStrip
        availableTools={availableTools}
        disabledTools={disabledTools}
        onToggle={toggleTool}
      />
```

Replace it with:
```tsx
      <ToolStrip
        availableTools={availableTools}
        disabledTools={disabledTools}
        onToggle={toggleTool}
        onInfo={() => setIsToolInfoOpen(true)}
      />
      {isToolInfoOpen && (
        <ToolInfoModal
          availableTools={availableTools}
          disabledTools={disabledTools}
          onClose={() => setIsToolInfoOpen(false)}
        />
      )}
```

- [ ] **Step 4: Verify TypeScript compiles with zero errors**

```
cd "C:/Users/Lenovo/IdeaProjects/agent-suite/frontend" && npx tsc -b --noEmit
```

Expected: no output (zero errors).

- [ ] **Step 5: Commit**

```
git -C "C:/Users/Lenovo/IdeaProjects/agent-suite" add frontend/src/App.tsx
git -C "C:/Users/Lenovo/IdeaProjects/agent-suite" commit -m "feat: wire ToolInfoModal into App — info button opens tool detail modal"
```
