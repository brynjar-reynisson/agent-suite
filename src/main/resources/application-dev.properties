server.port=8090
spring.datasource.url=jdbc:postgresql://127.0.0.1:54322/postgres
spring.datasource.username=postgres
spring.datasource.password=${SUPABASE_DB_PASSWORD:postgres}
supabase.jwt-secret=${SUPABASE_JWT_SECRET:}
supabase.url=http://127.0.0.1:54321

# Separate log file per environment so the concurrently-running dev and prod backends don't interleave.
logging.file.name=./logs/agent-suite-dev.log
